# ==============================================================================
# AURA Virtual Try-On (VTO) — Google Colab & GPU Service Dependencies
# ==============================================================================
# Target Environment: Google Colab (Python 3.10-3.12, NVIDIA Tesla T4, CUDA 12.x)
# Architecture: Decoupled FASHN VTON v1.5 MMDiT + DWPose Wholebody ONNX Detector
# License: Apache-2.0 Commercially Permissive Stack (Zero proprietary hosted APIs)
#
# NOTE ON PYTORCH & CUDA:
# Google Colab environments come with pre-installed PyTorch (torch, torchvision)
# compiled against CUDA 12.x. To avoid breaking Colab's pre-configured GPU runtime
# or triggering CPU-only wheel downgrades, torch and torchvision are NOT reinstalled
# via pip. If installing in a clean bare-metal environment outside Colab, install:
#   torch>=2.2.0
#   torchvision>=0.17.0
# ==============================================================================

# 1. FastAPI & High-Performance ASGI Web Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.10.0,<3.0.0

# 2. Authentication & Cloud Storage Integration (Supabase ES256/HS256)
python-jose[cryptography]==3.3.0
supabase==2.10.0

# 3. Neural Pipeline & Tensor Operations (MMDiT 972M)
safetensors>=0.5.0,<=0.8.0
einops>=0.8.0,<=0.8.2

# 4. ONNX Runtime for DWPose (Wholebody Pose & Keypoint Detection on CUDA T4)
# Note: onnxruntime-gpu provides CUDAExecutionProvider for YOLOX and U-COCO models.
onnxruntime-gpu==1.20.2

# 5. Vision & Computer Vision Operations (Headless for Colab/Server compatibility)
opencv-python-headless>=4.8.0,<=4.11.0.86
pillow>=10.2.0,<=11.1.0
numpy>=1.24.0,<3.0.0
matplotlib>=3.7.0,<=3.10.0

# 6. Model Weight Retrieval & Progress Tracking
huggingface_hub>=0.23.0
tqdm>=4.66.0

# 7. Authenticated Public Reverse Tunnel for Mobile App Integration
pyngrok>=7.0.0
